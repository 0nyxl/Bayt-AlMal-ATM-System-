# Bayt-AlMal-ATM-System-
This project implements a complete command-line interface (CLI) ATM banking system using C++ and file stream operations for data persistence. It simulates core banking functions for multiple clients, ensuring data integrity and user authentication.
